```text
         ID : 0023
       Date : 2026.07.14
  Requester : Leedox
    Subject : 서비스데스크 홈 생성 및 기능 추가
     Status : In Progress
 Visibility : Public
```

Description :
```text
홈화면 제작  : 단순목록 내부용,  각각 링크
  (현재 홈은 .local\dev\scr-service_desk-home.jpg 참조)
추가할 기능 - repo 내에서가 아닌 web에서 프로세스를 돌리기 위함
  티켓 발행기능 : 폼제공, md파일 생성, 목록 자동갱신
  작업내용 추가 : 팀원(AI에이전트 포함)들이 작업결과를 운영서버에 저장

참고 : db 생성없이 기능구현의 가능 여부부터 검토후 의견 제시
그후 의견에 따라 handoff 요청 패키지 작성 예정
```

Job :
```text
0001 Claudox 2026.07.14 22:27
요청대로 구현 전에 feasibility 검토 먼저 수행 (DB 생성 없이 가능한지 중심), 의견
제시. 코드 변경 없음 — 아래 결론에 따라 Tommy 결정 후 handoff 패키지 작성 예정.

1. 홈 화면(목록) 건 — 위험 없음
   app/views/service_desk/index.html.erb를 보면 사이드바 목록은 이미 존재하지만
   `hidden lg:block`이라 모바일에서 안 보임. 그래서 index가 첫 번째 티켓의 전체
   본문만 보이는 것처럼 보임(스크린샷과 일치). 순수 프론트엔드 수정, 아키텍처
   영향 없음.

2. 웹에서 티켓 발행 / Job 추가 — DB 유무와 무관하게 걸리는 것 3가지
   a. Railway 기본 파일시스템은 휘발성 — 재배포/재시작 시 로컬에 쓴 파일이
      사라짐. 웹에서 만든 티켓이 살아남으려면 Railway Volume(영구 볼륨)을
      서비스에 붙여야 함. 이건 코드가 아니라 인프라 설정(Railway 대시보드)이라
      Tommy 쪽 작업/승인이 필요.
   b. hq/service-desk/는 sync_curriculum.sh가 `rsync -a --delete`로 HQ
      스냅샷과 정확히 일치시키는 읽기 전용 미러 — 웹이 그 폴더에 직접 쓰면
      다음 sync 실행 때 삭제됨. 웹 작성분은 별도 디렉토리(볼륨 마운트 경로 등)에
      저장하고 화면에서 두 소스를 합쳐 보여줘야 함.
   c. 웹에서 만든 내용을 HQ(git)로 되돌리는 반대 방향 동기화가 현재 없음.
      REQ 0022 기록에 있던 sync_handoff.sh/push_handoff_to_curriculum.sh는
      실제로는 존재하지 않음(0022 티켓 본문에만 언급된 이름, 재확인 결과 두
      저장소 어디에도 실제 파일 없음) — 재사용 가능한 기존 패턴이 없으므로
      새로 설계 필요. 이게 없으면 "웹에서 생긴 기록이 HQ git에는 안 남는" 상태가
      되어 서비스데스크가 원래 지키던 "기록을 잃지 않는다" 원칙과 어긋남.

3. 동시 쓰기 — 여러 팀원/AI 에이전트가 같은 티켓 파일에 동시에 Job을 추가하면
   파일 쓰기 경합 가능(DB면 트랜잭션으로 자연 해결). 현재 규모(내부 소수)에서는
   실무적 위험은 낮다고 판단.

4. 권한 — ServiceDeskController에 이미 authenticate_user! + authorize :admin,
   :access? 가 걸려있어 쓰기 액션도 같은 가드 재사용 가능, 추가 작업 불필요.

의견: "DB 없이 가능한가"의 답은 기술적으로는 Yes이지만, 진짜 장애물은 DB
유무가 아니라 (a) Railway Volume, (b) 읽기전용/쓰기가능 폴더 분리,
(c) 웹→HQ git 역방향 동기화 3가지. 특히 (c)를 지키려면 오히려 가벼운 DB
테이블 하나가 파일보다 더 안전할 수 있다는 점(동시쓰기 안전, "HQ로 내보내기"
배치 작업이 더 깔끔)도 참고용으로 같이 제시 — 최종 판단은 Tommy.

0002 Claudox 2026.07.14 22:41
Tommy 방향 확정: git 기반 service-desk(HQ)와 DB 기반 service desk(DEV, 신규
구축)는 서로 SYNC하지 않는다 — 완전히 분리된 두 시스템으로 간다. Phase 4
(AI 에이전트 인증)와 Phase 5(HQ export)가 통째로 사라져 견적이 대폭 축소됨
(3~5세션 -> 약 3세션).

git 쪽(HQ service-desk/)의 "따로 정의할 용도"는 아직 Tommy 확답 전 —
Claudox가 chatdox/git_document_guidelines.md의 Official/Local/Ephemeral
분류를 재사용해 "git = 굵직한 의사결정/정책 기록용(No Ticket 기본값 체크리스트
그대로), DB = 웹 기반 팀+AI 운영 프로세스용"으로 제안, Tommy 확인 대기 중.
확정되면 GUIDE.md에도 반영 예정.

단일 장애점 리스크(Railway 프로젝트 하나가 유일한 사본이 됨) 관련해서는
실시간 동기화 대신 "DB 전체를 markdown zip으로 export"하는 수동 백업 버튼만
제안, 별도 결정 대기.

0003 Claudox 2026.07.14 22:49
Tommy 최종 확정 사항 반영, handoff 패키지 작성 완료.

- git 쪽 용도 정의 확정: git(HQ) = 의사결정/정책 기록용, DB(DEV) = 웹 기반
  팀+AI 운영 프로세스용. 두 시스템은 코드/데이터 어느 쪽으로도 동기화하지 않음.
- export(수동 백업 버튼) 포함 확정.
- 기존 23건 중 마이그레이션 대상 분류 확정(Claudox 제안 그대로, 0002/0018
  포함): 이관 14건(0001, 0003, 0004, 0005, 0006, 0008, 0009, 0010, 0011,
  0012, 0013, 0017, 0018, 0019, 0020) / git 유지 9건(0002, 0007, 0014,
  0015, 0016, 0021, 0022, 0023) — "빈 깡통보다 낫다"는 Tommy 판단대로 처음부터
  실제 히스토리를 갖춘 상태로 DB 시작.
- handoff 패키지 작성 완료: `.local/handoff/inbox/leedox_service_desk_db_r1/
  request.md` — 스키마(A), 14건 마이그레이션(B), 홈/목록 모바일 버그 수정(C),
  발행 웹폼(D), Job 추가 웹폼+AI 에이전트용 bearer 토큰 API(E), 인증(F),
  수동 export(G) 전 범위 포함. chatdox-platform(DEV)의 기존 `hq/service-desk/`
  파일 미러와는 완전히 독립적으로 설계 — 서로 건드리지 않음.
- 다음 단계: Platform Agent가 패키지 실행 → 구현 완료 후 결과 검토 →
  Tommy 확인되면 Confirmed.
```
