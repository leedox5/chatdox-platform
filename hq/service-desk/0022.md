```text
         ID : 0022
       Date : 2026.07.14
  Requester : Leedox
    Subject : GitHub repository 운영 변경 - subtree 삭제 및 local sync 방식
     Status : Completed
 Visibility : Private
```

Description :
```text
- subtree 방식은 자주 conflict 상황이 자주 만들어저 이를 페지
- HQ 에서 DEV로 넘겨야하는 컨텐트는 local copy로 sync한후 DEV에서 커밋
> 여기에 필요한 작업 순서와 수행자 선정
```

Job :
```text
0001 Claudox 2026.07.14 17:34
scope: Tommy 요청대로 subtree 전환만 처리 (문서 정리는 범위 밖, 후속 작업으로 남김).

1. DEV(chatdox-platform)에 script/sync_curriculum.sh 신설
   - `git -C <HQ> archive <ref> | tar -x`로 HQ의 git-tracked 스냅샷을 그대로 추출 후
     rsync -a --delete로 docs/curriculum/에 미러 (subtree reset 때 겪은 잔여 파일
     문제 재발 방지)
   - 기존 script/sync_handoff.sh, push_handoff_to_curriculum.sh와 동일한 rsync 기반
     패턴 재사용
2. --dry-run으로 먼저 검증: rsync itemize는 전체 파일을 "다름"으로 표시했으나
   (타임스탬프 노이즈), git archive 스냅샷과 실제 diff -rq 비교 결과 진짜 다른 파일은
   claudox/97_commands.md, claudox/memory_seed.md 2개뿐임을 확인 후 Tommy에게 공유
3. 실제 동기화 실행 -> git diff에도 정확히 그 2개 파일만 잡힘 확인
4. DEV에서 커밋(01fdfa8) 후 push 시도 -> origin/main이 이미 앞서있어 reject됨
   (Platform Agent가 leedox_nav_fixes_r1 관련 커밋 2개를 그 사이 푸시함)
5. git fetch + git merge origin/main -> 충돌 없이 자동 병합(12895e7) 확인 후
   Tommy 승인 받아 push 완료
6. git subtree는 submodule과 달리 별도 config/메타데이터가 없어 "해제" 액션이
   불필요함을 확인 -> 이번 일반 커밋 자체가 subtree 사용 중단의 실질적 시작점

후속 작업(이번 범위 밖, 별도 처리 필요):
- README.md, SETUP/01, SETUP/02, SETUP/README.md, TIPS/02, TIPS/03, TIPS/05,
  docs/05_project_structure.md — 아직 subtree를 현재 방법으로 안내 중, 새 스크립트
  방식으로 갱신 필요 (SETUP 01/02는 특히 복붙용 git subtree add/pull 명령어 포함)
- QA/01_github_strategy.md — subtree를 선택한 원래 이유가 담긴 의사결정 기록이라
  삭제 대신 "REQ 0022로 방식 변경" 메모만 추가 권장

0002 Claudox 2026.07.14 17:50
Tommy 지적으로 scope 추가 확장: DEV가 HQ 전체(docs/curriculum/)를 미러링하고
있었지만 실제로 읽는 건 3개 폴더뿐이었음이 드러남.

- DocsController/ClaudoxController/ClaudoxProductsController/ServiceDeskController
  4곳의 경로 상수 확인 -> 전부 docs/curriculum/{docs,claudox,service-desk/requests}
  3개만 실제로 읽음. QA/, SETUP/, TIPS/, chatdox/, prompts/, internal/, service-desk
  도구 스크립트 등은 DEV 앱이 전혀 안 씀.
- sync_curriculum.sh를 폴더별 개별 동기화로 재작성:
    HQ docs/                 -> DEV docs/chatdox/
    HQ claudox/               -> DEV docs/claudox/
    HQ service-desk/requests/ -> DEV service-desk/
- 4개 컨트롤러 경로 상수를 새 위치로 갱신, 기존 docs/curriculum/ 트리 삭제
- DEV가 이 PC에 두 벌 있음을 발견: Windows 마운트 클론(작업한 곳)과 WSL 네이티브
  클론(~/dev/chatdox-platform, bin/rails가 CRLF 없이 정상 실행되는 곳) -> 후자로
  pull 받아 테스트
- bin/rails test: 12 runs, 118 assertions, 0 failures (WSL 네이티브 클론에서 실행)
- 커밋 2개 푸시(d01cdd8 구조 변경, e1dafc0 service-desk/ add 누락분 추가 커밋)

실제 서버 기동 후 curl 스모크 테스트는 이 세션의 WSL 백그라운드 프로세스 처리가
불안정해 생략, 자동 테스트로 대체(라우트/컨트롤러를 실제로 태우는 성격은 동일).
```
