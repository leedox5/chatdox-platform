```text
         ID : 0018
       Date : 2026.07.12
  Requester : Claudox
    Subject : [SUB-C/0014] claudox+서비스데스크 웹 뷰어 구현
     Status : Confirmed
 Visibility : Public
```

Description :
```text
상위 요청: 0014
선행 요청: 0015 (완료), 0016 (완료)

내용: SUB-B(0015)에서 정한 접근 정책, SUB-A(0016)에서 정한 공개 범위를
바탕으로 claudox/, service-desk/ 컨텐츠를 실제 웹(chatdox-platform)에서
볼 수 있게 구현한다. docs/ 폴더가 이미 같은 방식으로 렌더링되고
있으니 그 구조를 그대로 참고한다.

범위 밖: 실제 코드 수정은 chatdox-platform 리포(Rails 앱)에서 이뤄져야
한다. 이 리포(chatdox-curriculum)는 소스만 갖고 있고, 이 PC에는
chatdox-platform이 아직 clone되어 있지 않아 지금 당장 구현을 시작할
수 없다 (REQ 0005와 동일한 사유). chatdox-platform 경로가 확인되면
Status를 In Progress로 올리고 아래 체크리스트대로 진행한다.

구현 체크리스트:
- [ ] docs/curriculum과 같은 방식으로 claudox/, service-desk/를
      chatdox-platform에 git subtree add
- [ ] claudox 챕터 뷰어 라우트/뷰 (docs/ 렌더러 재사용 가능한지 검토)
- [ ] 서비스데스크 dashboard.md 뷰어 라우트/뷰
- [ ] Visibility : Private 요청은 웹에서 제외하는 필터링 로직
      (현재 0014, 0015가 Private)
- [ ] 접근 등급 게이팅 (SUB-B/0015 결정 반영)
      - 게스트(비로그인): claudox 1~2장만, 서비스데스크 비공개
      - 무료체험(7일): claudox 1~5장, 서비스데스크는 dashboard.md
        요약만(개별 요청 상세 비공개)
      - 구독자: claudox 전체 20장 + 서비스데스크 전체 공개
- [ ] REQ 0005(표 렌더링: Redcarpet tables:true, Tailwind Typography)가
      먼저 해결되어야 claudox 챕터 안의 표도 안 깨짐 — 선행 의존성으로만
      참고, 별도 티켓이라 여기서 중복 처리하지 않음
```

Job :
```text
0001 Claudox 2026.07.12 19:35
접수. chatdox-platform 리포가 이 PC에 없어 실제 구현은 보류, 구현
체크리스트만 먼저 정리해서 New로 남김 (Leedox 확인: "티켓만 먼저
접수"). chatdox-platform 경로가 확인되는 대로 In Progress로 전환.

0002 Claudox 2026.07.12 21:48 (chatdox-platform에서 처리, sync-platform.ps1로 가져옴)
- chatdox-platform이 워크스페이스에서 확인되어 In Progress로 전환,
  구현 착수
- claudox/service-desk 전용 라우트와 뷰 뼈대를 docs 렌더러 패턴을
  재사용하는 방식으로 추가 시작

0003 Codidox 2026.07.12 23:07 (chatdox-platform에서 처리, sync-platform.ps1로 가져옴)
- claudox/service-desk 첫 진입을 요약 카드 없이 좌측 전체 요청 목록 +
  우측 본문 구조로 정리 (상세 화면도 같은 좌측 목록 유지)
- docs/curriculum/service-desk/requests/NNNN.md 형식 문서만 목록에
  노출하도록 request 스캐너 구성, 기본 진입 시 첫 요청 자동 선택
- page smoke test로 /service-desk 응답과 sidebar 렌더링 확인

0004 Leedox 2026.07.12 23:34 (chatdox-platform에서 확인, sync-platform.ps1로 가져옴)
- claudox/service-desk 화면 진입 구조와 사이드바 폭 통일을 웹에서 최종
  확인, 0018 처리 결과 승인 -> platform 쪽 미러도 Confirmed로 마감

0005 Claudox 2026.07.12 23:42
platform 쪽 request/0018_handoff_to_curriculum.md 확인 - "현재 curriculum
저장소에 즉시 반영해야 할 새 수정사항 없음"으로 명시되어 있어 추가
조치 없이 원본(이 요청)도 동일하게 Confirmed 처리. SUB A(0016)/B(0015)/
C(0018) 모두 Confirmed 완료 -> 상위 요청 0014 갱신 필요
```
