```text
         ID : 0005
       Date : 2026.07.12
  Requester : Claudox
    Subject : docs/01_overview.md 기술 스택 표가 chatdox-platform에서 안 깨진 채로 렌더링되도록 수정
     Status : Confirmed
 Visibility : Public
```

Description :
```text
증상: docs/01_overview.md의 "기술 스택" 표(및 다른 | 표들)가 chatdox-platform
웹앱에서 렌더링될 때 표 형태(테두리/구분선) 없이 텍스트가 한 덩어리로
뭉쳐 보인다. 볼드(**언어** 등)는 살아있는 걸로 보아 <table> 자체는
파싱되지만 스타일이 전혀 적용되지 않은 것으로 추정.

원인 추정 (TIPS/04_redcarpet_guide.md 기준):
1. Redcarpet::Markdown.new(renderer, ...)에 tables: true 옵션이
   꺼져있어서 표 자체가 파싱 안 될 가능성
2. 표는 파싱되더라도, 뷰의 <div class="prose">에 Tailwind Typography
   플러그인(@tailwindcss/typography)이 적용 안 돼있어서 <table>이
   무스타일로 나오는 경우

범위 밖: docs/*.md는 chatdox-curriculum -> chatdox-platform으로
subtree pull만 되는 읽기 전용 소스라서, 실제 수정은 chatdox-platform
리포(마크다운 렌더링 헬퍼 + CSS)에서 이뤄져야 한다.
(TIPS/03_subtree_collaboration.md 참고)

체크리스트:
- [ ] chatdox-platform의 Redcarpet::Markdown.new 호출에 tables: true 확인/추가
- [ ] .prose 컨테이너에 @tailwindcss/typography 플러그인 적용 여부 확인
- [ ] 확인 후 docs/01_overview.md 등 표가 포함된 문서로 실제 렌더링 테스트
```

Job :
```text
0001 Claudox 2026.07.12 16:21
- bundle-install.txt 로그에서 chatdox-platform이 tailwindcss-rails
  4.x(Tailwind CSS v4)를 쓴다는 게 확인됨 -> 원인 추정 (2)의 처방이
  v3 방식(npm install + tailwind.config.js)이라 안 맞았음을 발견
- prompts/05_docs_preview.md의 "5️⃣ Tailwind Prose 스타일" 섹션을 v4
  방식으로 갱신: npm 불필요, app/assets/tailwind/application.css에
  `@plugin "@tailwindcss/typography";` 한 줄만 추가하면 됨
  (tailwindcss-rails 내장 standalone CLI가 1st-party 플러그인을
  이름으로 인식)
- 구현 체크리스트에 Redcarpet tables: true 항목도 같이 정리해서,
  chatdox-platform에서 실제 고칠 때 참고할 단일 체크리스트로 정비
- 여전히 실제 수정은 chatdox-platform 리포에서 해야 함 (범위 밖은
  그대로 유효) - Status는 진행 중으로 유지

0002 Codidox 2026.07.12 21:39 (chatdox-platform에서 처리, REQ 0019로 가져옴)
- table 렌더링 문제 재현 확인: docs_controller는 tables: true였지만,
  docs 뷰의 기본 sanitize가 table/th/td 태그를 제거하고 있었음
  (원인 추정 1/2 둘 다 아니고 제3의 원인 - sanitize 허용 태그 누락)
- app/views/docs/show.html.erb의 sanitize 허용 태그에
  table/thead/tbody/tr/th/td 추가해 표 구조가 유지되도록 수정
- app/assets/tailwind/application.css에
  @plugin "@tailwindcss/typography" 추가해 Tailwind v4 typography 활성화
- rails runner로 sanitize 제거 이슈를 수치로 확인 후, 수정본에서 table
  태그가 유지되는지 재검증

0003 Leedox 2026.07.12 21:48 (chatdox-platform에서 확인, REQ 0019로 가져옴)
- 웹에서 실제 렌더링 결과 확인 후 처리 결과 승인, Status를 Completed
  에서 Confirmed로 변경 (원본인 이 요청도 동일하게 Confirmed 처리)

0004 Claudox 2026.07.12 22:12 (chatdox-platform 핸드오프 메모 피드백, REQ 0019)
platform 쪽 request/0005_handoff_to_curriculum.md로 넘어온 "curriculum 측
전달 필요 가능 항목" 3개에 대한 검토 결과:

1. Tailwind v4 typography 가이드 정비 요청
   -> 이미 반영됨. prompts/05_docs_preview.md "5️⃣ Tailwind Prose
      스타일" 섹션이 이 요청(0005) Job 0001 시점에 v4 방식(CSS
      플러그인 등록, npm 불필요)으로 이미 갱신돼 있었음. 추가 조치 불필요.

2. Redcarpet 표 렌더링 주의사항(tables:true + sanitize 허용 태그) 정리 요청
   -> 신규 정보였음. sanitize가 table류 태그를 지운다는 사실은 이번에
      처음 확인된 원인이라 curriculum 쪽엔 기록이 없었음. TIPS/04_
      redcarpet_guide.md에 "Tip 4: sanitize()로 한 번 더 걸러낼 때
      표가 사라지는 경우" + FAQ 항목으로 반영 완료 (이 요청의 실제
      사례로 인용).

3. subtree read-only 정책 안내 강화 요청
   -> 이미 충분히 명시돼 있다고 판단. TIPS/02_git_subtree.md의 Tip 2
      (Subtree 폴더의 약속), Tip 4(대량 충돌), Tip 5(reset)와 프로젝트
      CLAUDE.md에 이미 "docs/curriculum은 읽기 전용, 수정은 curriculum
      쪽에서" 원칙이 여러 번 명시돼 있음. 추가 조치는 보류 - 구체적으로
      어느 지점에서 정책이 안 보이거나 헷갈렸는지 사례가 있으면 알려주면
      그 지점을 보강하는 게 더 정확할 것 같음.
```
