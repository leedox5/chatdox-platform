```text
         ID : 0008
       Date : 2026.07.12
  Requester : Claudox
    Subject : dashboard.md 자동 생성 스크립트 추가
     Status : Confirmed
 Visibility : Public
```

Description :
```text
문제: dashboard.md의 종합현황(요청/신규/진행중/완료/확인 건수)과 표를
매번 Claudox가 requests/*.md를 손으로 읽고 계산해서 다시 쓰고 있음.
요청이 늘어날수록 계산 실수/오타 위험이 커짐.

제안: new.sh/new.ps1과 같은 방식으로 requests/*.md를 스캔해서
dashboard.md를 자동 생성하는 status.sh / status.ps1 스크립트 추가.
각 파일의 Status 필드를 읽어 상태별 건수를 집계하고, ID/Date/Subject/
Status 표까지 통째로 재생성한다.

작업 범위:
1. status.sh (Git Bash), status.ps1 (PowerShell) 작성
2. requests/*.md에서 ID, Date, Subject, Status 파싱
3. dashboard.md를 덮어쓰기 (요청 N · 신규 N · 진행중 N · 완료 N · 확인 N + 표)
4. GUIDE.md에 사용법 문서화
```

Job :
```text
0001 Claudox 2026.07.12 17:00
- status.sh, status.ps1 작성: requests/*.md의 ID/Date/Subject/Status를
  파싱해 dashboard.md(종합현황 + 표)를 통째로 재생성
- 버그 발견 및 수정: status.ps1이 헤더 필드 값을 깨진 한글로 써서
  dashboard.md를 오염시킴. 원인 두 가지 -
  (1) status.ps1 자체가 BOM 없는 UTF-8이라 PowerShell 5.1이 스크립트
      안의 한글 리터럴을 잘못된 코드페이지로 파싱
  (2) Get-Content가 -Encoding 없이 BOM 없는 요청 파일을 읽어서 내용이
      깨짐
  -> status.ps1/new.ps1에 UTF-8 BOM 추가, Get-Content에 -Encoding UTF8
     명시로 해결. bash/PowerShell 둘 다 재테스트해서 정상 출력 확인
- GUIDE.md에 "dashboard.md 자동 갱신" 섹션 추가, 프로젝트 CLAUDE.md도 갱신
```
